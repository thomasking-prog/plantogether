<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Home</a>
    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('admin_dashboard') }}" class="navbar-text">
            Admin
        </a>
    {% endif %}

    <div class="ms-auto d-flex align-items-center gap-2">


        {% if app.request.attributes.get('_route') == 'home' %}
            <span class="navbar-text">Vous êtes sur la page d'accueil</span>
        {% elseif app.request.attributes.get('_route') == 'project_show' %}
            <button class="btn btn-outline-secondary navbar-text" id="projectMembers" title="Gérer les membres">
                <i class="bi bi-people"></i>
            </button>
        {% endif %}

    </div>
</nav>
